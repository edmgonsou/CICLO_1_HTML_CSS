1. Como podemos definir uma URL, dê um exemplo.
    RESP.: é definido como conjunto de elementos que corresponde ao protocolo HTTP
           ao endereço de dominio e as pastas destes dominio.
           exemplo http//homenaranha/homen_araha
2. O que é o HTTP?
    RESP.: é um protocolo de comunicação entre o navegador eo servidor do webserv
3 O que são vendors em CSS?
    RESP.: são prefixo que os fabricante de browser dispoe noCSS para testar novas funcionalidades
    do CSS